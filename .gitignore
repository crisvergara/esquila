# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
counts/*
esquila
*.sqlite

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
/playwright-report
/test-results

# production
/build
/build-cloud
/dist-mac
/cloud/node_modules

# misc
.DS_Store

# env files hold real tokens — never commit any of them
.env
.env.*

# runtime artifacts if the server is ever run from the repo directory
.onboarded

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Generated release identity and cloud update feed (CI only)
/mac/release.json
/cloud/mac-update.json
/release-assets
